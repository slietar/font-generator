image: node:latest

before_script:
  - npm install

build:
  stage: build
  script:
    - npm run build
  artifacts:
    paths:
      - public

pages:
  stage: deploy
  script:
    - ":"
  artifacts:
    paths:
      - public
  only:
    - master

cache:
  paths:
    - node_modules

